# Keycloak issuer URI (your realm)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/frontend-calendar-realm

# Server port for your calendar service
server.port=9090

# Spring Security logging level (INFO is for production; DEBUG for development)
logging.level.org.springframework.security=INFO
